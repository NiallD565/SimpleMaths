<Page
    x:Class="MathsGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MathsGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        </Grid>
        <Grid Grid.Row="0" Margin="0 20 0 0">
            <TextBlock x:Name="Title" Text="Simple Maths" Foreground="white"
                       TextWrapping="Wrap" FontSize="50" VerticalAlignment="Top" HorizontalAlignment="Center" TextAlignment="Center"/>
            

        </Grid>
        <Grid Grid.Row="1">
            <TextBlock x:Name="txtHighscore" Text="Highscore: 0" Foreground="White" FontWeight="ExtraBold" TextWrapping="Wrap"
                       FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="2" Margin="100">
            <StackPanel Orientation="Vertical">
                <Button x:Name="btnStart" BorderBrush="#ffffff" BorderThickness="2" Background="DarkBlue" Content="Play" HorizontalAlignment="Center"
                        VerticalAlignment="Center" Padding="40 10 40 10" Foreground='AliceBlue' FontWeight="ExtraBold" FontSize="30" Click="btnStart_Click"/>
                <Button x:Name="btnOption" BorderBrush="#ffffff" BorderThickness="2" Margin="0 20 0 0" Background="DarkBlue" Content="Option" HorizontalAlignment="Center"
                        VerticalAlignment="Center" Padding="20 10 20 10" Foreground='AliceBlue' FontWeight="ExtraBold" FontSize="30" Click="btnOption_Click"/>
            </StackPanel> 
            
        </Grid>
        
    </Grid>
</Page>
